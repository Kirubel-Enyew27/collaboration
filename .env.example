# Example environment variables for local/deployment
# Database file path (inside container)
DATABASE_PATH=/data/data.db

# API key example (used by the built-in dev auth middleware)
DEV_API_KEY=dev-key
